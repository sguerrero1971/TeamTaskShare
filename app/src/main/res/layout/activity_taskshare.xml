<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:gravity="center"
    android:orientation="vertical"
    tools:context=".TaskShareActivity" >

    <TextView
        android:id="@+id/profile_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/ParseLoginUI.TextView"
        android:text="@string/profile_title_logged_in" />

    <TextView
        android:id="@+id/profile_name"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/ParseLoginUI.TextView" />

    <TextView
        android:id="@+id/profile_email"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        style="@style/ParseLoginUI.TextView" />

    <Button
        android:id="@+id/login_or_logout_button"
        style="@style/ParseLoginUI.Button"
        android:layout_marginTop="@dimen/com_parse_ui_large_vertical_spacing"
        android:text="@string/profile_login_button_label"/>

    <Button
        android:id="@+id/get_list_button"
        style="@style/ParseLoginUI.Button"
        android:layout_marginTop="@dimen/com_parse_ui_large_vertical_spacing"
        android:text="@string/get_list_button"/>

    <ListView
        android:id="@+id/taskShare_list_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@string/get_list_button" >
    </ListView>

    <LinearLayout
        android:id="@+id/no_taskShares_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:gravity="center_vertical|center"
        android:orientation="vertical" >

        <TextView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fontFamily="sans-serif"
            android:gravity="center"
            android:text="@string/no_items"
            android:textAlignment="center"
            android:textSize="20sp" />
    </LinearLayout>

</LinearLayout>